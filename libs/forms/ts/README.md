# forms-ts

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build forms-ts` to build the library.

## Running unit tests

Run `nx test forms-ts` to execute the unit tests via [Vitest](https://vitest.dev/).
