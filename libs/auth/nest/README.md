# auth-nest

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build auth-nest` to build the library.

## Running unit tests

Run `nx test auth-nest` to execute the unit tests via [Jest](https://jestjs.io).
