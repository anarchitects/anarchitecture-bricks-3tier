# contacts-nest-presentation

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build contacts-nest-presentation` to build the library.

## Running unit tests

Run `nx test contacts-nest-presentation` to execute the unit tests via [Jest](https://jestjs.io).
