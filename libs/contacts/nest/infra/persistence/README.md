# contacts-nest-infra-persistence

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build contacts-nest-infra-persistence` to build the library.

## Running unit tests

Run `nx test contacts-nest-infra-persistence` to execute the unit tests via [Jest](https://jestjs.io).
