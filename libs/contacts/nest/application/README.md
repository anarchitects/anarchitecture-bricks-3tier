# contacts-nest-application

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build contacts-nest-application` to build the library.

## Running unit tests

Run `nx test contacts-nest-application` to execute the unit tests via [Jest](https://jestjs.io).
