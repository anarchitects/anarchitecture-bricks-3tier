# contacts-ts-models

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build contacts-ts-models` to build the library.

## Running unit tests

Run `nx test contacts-ts-models` to execute the unit tests via [Vitest](https://vitest.dev/).
